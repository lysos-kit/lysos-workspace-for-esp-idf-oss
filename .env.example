# ============================================================================
# ESP-IDF Docker Environment Configuration
# ============================================================================
# Copy this file to .env and customize as needed
# DO NOT commit .env to version control (it may contain secrets)
# ============================================================================

# ============================================================================
# DOCKER USER CONFIGURATION
# ============================================================================
# User and group IDs for the container (should match your host user)
# On Linux/WSL2: Run `id -u` and `id -g` to get your user/group ID
USER_ID=1000
GROUP_ID=1000

# ============================================================================
# ESP-IDF TARGET CONFIGURATION
# ============================================================================
# Target chip for your project
# Options: esp32, esp32s2, esp32s3, esp32c2, esp32c3, esp32c6, esp32h2
IDF_TARGET=esp32s3

# ============================================================================
# SERIAL PORT CONFIGURATION
# ============================================================================
# Serial port for flashing and monitoring
# Windows/WSL2: Usually /dev/ttyUSB0 or /dev/ttyACM0
# Linux: /dev/ttyUSB0, /dev/ttyUSB1, etc.
# macOS: /dev/cu.usbserial-* or /dev/cu.SLAB_USBtoUART
ESPPORT=/dev/ttyUSB0

# Baud rate for flashing
# Default: 115200, Higher speeds: 230400, 460800, 921600 (may not work on all systems)
ESPBAUD=115200

# Baud rate for serial monitor
# Should match your firmware's UART configuration
MONITORBAUD=115200

# ============================================================================
# BUILD CONFIGURATION
# ============================================================================
# Enable ccache to speed up rebuilds (0=disabled, 1=enabled)
IDF_CCACHE_ENABLE=1

# CCache directory (inside container)
CCACHE_DIR=/root/.ccache

# CMake build type
# Options: Debug, Release, RelWithDebInfo, MinSizeRel
CMAKE_BUILD_TYPE=Release

# Parallel build jobs (empty = auto-detect, or specify number like 4, 8, etc.)
# Higher values speed up compilation but use more RAM
IDF_BUILD_PARALLEL=

# ============================================================================
# FLASH CONFIGURATION
# ============================================================================
# Flash mode
# Options: qio, qout, dio, dout
# qio = fastest but not all chips support it
# dio = recommended default for ESP32-S3
ESPFLASHMODE=dio

# Flash frequency
# Options: 80m, 40m, 26m, 20m
# 80m = fastest, but check your hardware compatibility
ESPFLASHFREQ=80m

# Flash size
# Options: detect, 2MB, 4MB, 8MB, 16MB, 32MB
# detect = auto-detect (recommended)
ESPFLASHSIZE=detect

# ============================================================================
# MONITOR CONFIGURATION
# ============================================================================
# Enable automatic decoding of backtraces and panic output (0=disabled, 1=enabled)
IDF_MONITOR_DECODE=1

# Monitor baud rate (should match MONITORBAUD, kept for compatibility)
IDF_MONITOR_BAUD=115200

# ============================================================================
# DEBUG CONFIGURATION
# ============================================================================
# OpenOCD commands for debugging (advanced users)
# Example: -f board/esp32s3-builtin.cfg
OPENOCD_COMMANDS=

# OpenOCD debug level (0-4, higher = more verbose)
OPENOCD_DEBUG_LEVEL=

# ============================================================================
# PYTHON ENVIRONMENT
# ============================================================================
# Custom Python environment path (leave empty to use default ESP-IDF env)
IDF_PYTHON_ENV_PATH=

# ============================================================================
# COMPONENT CONFIGURATION
# ============================================================================
# Additional component directories (semicolon-separated paths)
# Example: /workspace/components;/workspace/external_components
EXTRA_COMPONENT_DIRS=

# ============================================================================
# COMPILER OPTIMIZATION FLAGS
# ============================================================================
# Custom C compiler flags
# Example: -O2 -g -Wall
CFLAGS=

# Custom C++ compiler flags
# Example: -O2 -g -Wall -std=c++17
CXXFLAGS=

# Custom linker flags
# Example: -Wl,--gc-sections
LDFLAGS=

# ============================================================================
# SECURITY CONFIGURATION (Advanced - Production Use)
# ============================================================================
# WARNING: DO NOT commit these keys to version control!
# Path to secure boot signing key (inside container)
SECURE_BOOT_SIGNING_KEY=

# Path to flash encryption key (inside container)
SECURE_BOOT_ENCRYPTION_KEY=

# ============================================================================
# ADVANCED SETTINGS
# ============================================================================
# ESP-IDF path (DO NOT change unless you know what you're doing)
# IDF_PATH=/opt/esp/idf

# Python version to use (leave empty for default)
# IDF_PYTHON=python3.11

# Git executable path (leave empty for default)
# IDF_GIT=git

# Enable verbose build output (V=0 for quiet, V=1 for verbose)
# V=0

# ============================================================================
# EXAMPLE CONFIGURATIONS FOR DIFFERENT SCENARIOS
# ============================================================================

# --- Development Mode ---
# CMAKE_BUILD_TYPE=Debug
# IDF_CCACHE_ENABLE=1
# ESPBAUD=921600
# IDF_MONITOR_DECODE=1

# --- Production Build ---
# CMAKE_BUILD_TYPE=Release
# IDF_CCACHE_ENABLE=0
# CFLAGS=-Os -DNDEBUG
# ESPFLASHMODE=dio
# ESPFLASHFREQ=80m

# --- Size-Optimized Build ---
# CMAKE_BUILD_TYPE=MinSizeRel
# CFLAGS=-Os -flto
# CXXFLAGS=-Os -flto

# --- Fast Flash Mode (for ESP32-S3 with compatible flash) ---
# ESPFLASHMODE=qio
# ESPFLASHFREQ=80m
# ESPBAUD=921600
